<h1>Tu będzie index</h1><br>
<%= link_to "Nowa", new_message_path %><br>

<% @messages.each do |message| %>
	<%= message.title %><br>
	<%= message.content %><br>
	<%= message.created_at %><br>
	<% end %>